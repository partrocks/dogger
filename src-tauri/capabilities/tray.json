{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray",
  "description": "Capability for the menu bar popover panel window",
  "windows": ["tray"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
